# 03_tracks_originais

Third-party genome tracks used as-is (not produced by this pipeline) — e.g.
CanFam3.1-based annotation used as a liftover source before ROS_Cfam_1.0
became the primary reference. Empty until a specific external track is
needed by a downstream step; populated on demand.
